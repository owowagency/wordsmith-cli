on:
  push:
    tags:   
      - 'v*'

name: Release

jobs:
  build:
    name: 📦 Build ${{ matrix.platform.target }}
    strategy:
      matrix:
        platform:
          - target: aarch64-apple-darwin
            os: macos-latest
          - target: x86_64-apple-darwin
            os: macos-latest
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            apt: aarch64-linux-gnu-gcc
          - target: x86_64-pc-windows-msvc
            os: windows-latest
    runs-on: ${{ matrix.platform.os }}
    steps:
      - uses: actions/checkout@v3
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
      - name: Add target
        run: rustup target add ${{ matrix.platform.target }}
      - name: Install APT dependencies
        if: ${{ matrix.platform.apt }}
        run: |
          sudo apt update
          sudo apt install -y ${{ matrix.platform.apt }}
      - name: 🏗️ Cargo build
        uses: actions-rs/cargo@v1
        with:
          command: build
          args: --features "strict" --release --target ${{ matrix.platform.target }}
      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: "wordsmith-${{ matrix.platform.target }}"
          path: "target/${{ matrix.platform.target }}/release/wordsmith"
          retention-days: 1
    
  release:
    needs: build
    name: 🚀 Release
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v3
        with:
          path: bin/
      - name: Create Github Release (Replace with semantic release)
        uses: softprops/action-gh-release@v1
        with:
          body: ''
          append_body: true
          files: |
              bin/wordsmith-aarch64-apple-darwin
              bin/wordsmith-x86_64-apple-darwin
              bin/wordsmith-x86_64-unknown-linux-gnu
              bin/wordsmith-aarch64-unknown-linux-gnu
              bin/wordsmith-x86_64-pc-windows-msvc
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

